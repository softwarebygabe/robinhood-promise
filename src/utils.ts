export function validateConfiguration(config: IRobinhoodClientConfig): void {
  // TODO - validate the config
  return
}

export function handleResponseErr(response: any): void {
  // TODO
  return
}
